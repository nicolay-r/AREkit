from arekit.contrib.source.common.labels import PositiveLabel, NegativeLabel

# Defaul label formattings.
POS_LABEL_STR = "pos"
NEG_LABEL_STR = "neg"

# Default label instances.
RUSENTREL_POS_LABEL = PositiveLabel()
RUSENTREL_NEG_LABEL = NegativeLabel()
